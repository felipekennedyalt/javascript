function antSuc() {
    let numero = window.prompt('Digite um número inteiro qualquer')

    window.alert(`Antes de ${Number(numero)}, temos o número ${Number(numero) - 1}.
Depois de ${Number(numero)}, temos o número ${Number(numero) + 1}`)
}